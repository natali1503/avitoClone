:80 {
    root * /usr/share/caddy
    encode gzip

    # Handle API requests
    handle /api/* {
        uri strip_prefix /api
        reverse_proxy backend:3000
    }

    # Serve static files
    handle {
        try_files {path} /index.html
        file_server
    }
}
